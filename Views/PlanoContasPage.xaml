<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BalancoPatrimonial.App.Views.PlanoContasPage"
             xmlns:vm="clr-namespace:BalancoPatrimonial.App.ViewModels"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto, *" Padding="16">
        <Label Text="Plano de Contas (De/Para)" FontSize="24" FontAttributes="Bold" Margin="0,0,0,16"/>
        <CollectionView Grid.Row="1" ItemsSource="{Binding Contas}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="8">
                        <Label Text="{Binding Codigo}" FontAttributes="Bold"/>
                        <Label Text="{Binding Descricao}"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
