# DAO SQLite

Implementações de DAO para SQLite (banco local de configurações).

**Fase 2:** Será criada:
- `SqliteConnectionFactory.cs`
- `SqliteConfiguracaoDao.cs` — armazena tema (claro/escuro), último filtro de pesquisa, último usuário logado, preferências em geral.

A base SQLite fica no diretório de dados do app (`FileSystem.AppDataDirectory`).
