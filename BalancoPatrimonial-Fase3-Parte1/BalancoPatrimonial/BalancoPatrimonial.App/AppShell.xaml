<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BalancoPatrimonial.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:BalancoPatrimonial.App.Views"
    Shell.FlyoutBehavior="Flyout"
    Shell.FlyoutBackgroundColor="{StaticResource SidebarBackground}"
    Shell.BackgroundColor="{StaticResource Primary}"
    Shell.ForegroundColor="White"
    Shell.TitleColor="White"
    FlyoutWidth="260"
    Title="Balanço Patrimonial">

    <!-- Cabeçalho do sidebar (mostra o usuário logado) -->
    <Shell.FlyoutHeader>
        <Grid HeightRequest="140"
              BackgroundColor="{StaticResource Primary}"
              Padding="20">
            <VerticalStackLayout VerticalOptions="Center" Spacing="4">
                <Label Text="Balanço Patrimonial"
                       TextColor="White"
                       FontSize="20"
                       FontAttributes="Bold"/>
                <Label Text="Análise de Crédito"
                       TextColor="#B8C5D6"
                       FontSize="12"/>
                <Label x:Name="lblUsuarioLogado"
                       Text="—"
                       TextColor="White"
                       FontSize="13"
                       Margin="0,12,0,0"/>
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <!-- Estilo dos itens do flyout -->
    <Shell.Resources>
        <Style x:Key="BaseStyle" TargetType="Element">
            <Setter Property="Shell.BackgroundColor" Value="{StaticResource Primary}" />
            <Setter Property="Shell.ForegroundColor" Value="White" />
            <Setter Property="Shell.TitleColor" Value="White" />
            <Setter Property="Shell.NavBarHasShadow" Value="False" />
        </Style>
        <Style TargetType="ShellContent" BasedOn="{StaticResource BaseStyle}" />
        <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
    </Shell.Resources>

    <!-- As 4 páginas acessíveis pelo sidebar -->
    <FlyoutItem Title="Planilhamento" Route="planilhamento">
        <ShellContent ContentTemplate="{DataTemplate views:PlanilhamentoPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Empresas" Route="empresas">
        <ShellContent ContentTemplate="{DataTemplate views:EmpresasPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Logs" Route="logs">
        <ShellContent ContentTemplate="{DataTemplate views:LogsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Área de Testes" Route="testes">
        <ShellContent ContentTemplate="{DataTemplate views:AreaTestesPage}" />
    </FlyoutItem>

    <!-- Item de logout no rodapé do flyout -->
    <MenuItem Text="Sair" Clicked="OnLogoutClicked" />

</Shell>
