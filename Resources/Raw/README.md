# Recursos brutos

Coloque arquivos que devem ser empacotados sem processamento (PDFs de exemplo,
JSONs de configuração default, etc.). Use `FileSystem.OpenAppPackageFileAsync()`
pra acessá-los em runtime.
